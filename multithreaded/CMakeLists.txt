IF (MYSQL_FOUND)
    add_executable(exact_mt exact_mt)
    target_link_libraries(exact_mt exact_common exact_strategy exact_image_tools ${UNDVC_COMMON_LIBRARY} ${MYSQL_LIBRARIES} pthread)
ELSE (MYSQL_FOUND)
    add_executable(exact_mt exact_mt)
    target_link_libraries(exact_mt exact_common exact_strategy exact_image_tools ${UNDVC_COMMON_LIBRARY} pthread)
ENDIF (MYSQL_FOUND)
